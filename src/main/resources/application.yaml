spring:
  datasource:
    url: jdbc:h2:file:./data/${NODE_ID};MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8080

app:
  node-id: ${NODE_ID:node-default}